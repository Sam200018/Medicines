import 'package:flutter/material.dart';

class NotAtHomePage extends StatelessWidget {
  const NotAtHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Not at Home"),
      ),
    );
  }
}
